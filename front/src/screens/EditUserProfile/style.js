import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  text: {
    fontSize: 50,
  },
  icon: {
    backgroundColor: "#ccc",
    position: "absolute",
    right: 0,
    bottom: -30,
  },
});
